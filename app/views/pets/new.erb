<h1>Create a new Pet</h1>

<form action="/pets" method="post">
    <label for="pet_name">Pet Name: </label>
    <input type="text" id="pet_name" name="pet[name]">
    <br>

    <h2>Select an existing owner or create one below!</h2>

    <h3>Choose an existing owner</h3>
        <% @owners.each do |owner|%>
        <input type="radio" name="pet[owner_id]" id="<%= owner.id %>" value="<%= owner.id %>"><%= owner.name %><br>
        <% end %>
        <br>


    <h3>Create a new owner</h3>
        <label>Owner Name: </label>
        <input type="text" name="owner[name]" id="owner_name">
        <br><br>
        <input type="submit" value="Create Pet">
    
</form>
